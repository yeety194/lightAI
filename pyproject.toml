[project]
name = "lightai"
version = "0.1.0"
description = "Standalone AI network assistant - runs completely locally without APIs"
dependencies = [
    "torch>=2.0.0",
    "transformers>=4.30.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"